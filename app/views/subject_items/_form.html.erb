<%= form_for(@subject_item) do |f| %>
  <% if @subject_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject_item.errors.count, "error") %> prohibited this subject_item from being saved:</h2>

      <ul>
      <% @subject_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.select :subject_id, Subject.all.map {|p| [p.subject,p.id]} %>
  </div>
  <div class="actions">
    <%= f.submit "Submit"%>
  </div>
<% end %>
